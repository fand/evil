'use strict';

var Timer = require('worker-timer');

const interval = 200;  // 100msec
const offset = 100;

class Sequencer {
    constructor () {

    }

    loadScene () {
        // var nextScene = this.scene.nextScene();
        // if (this.scene !== nextScene) {
        //     this.scene = nextScene;
        // }
    }

    play () {
        // if (this.scene.isEnd) {
        //     this.song.scenes();
        // }
        //
        // Timer.setTimeout(u => {this.play(u)}, interval);
    }
}

module.exports = Sequencer;
